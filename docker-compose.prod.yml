services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: clyvanta-prod
    ports:
      - "80:3000"
    env_file:
      - .env.production                 # Load production environment
    restart: always
    networks:
      - clyvanta-prod-network

networks:
  clyvanta-prod-network:
    driver: bridge
